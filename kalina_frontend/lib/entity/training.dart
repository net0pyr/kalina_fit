import 'package:flutter/material.dart';

class Training {
  String eventName;
  Color background;
  DateTime startTime;
  DateTime endTime;

  Training(this.eventName, this.background, this.startTime, this.endTime);
}
